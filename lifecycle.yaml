---
version: '1.0.0'

environment:
  autoDeploy: true
  defaultServices:
    - name: 'remote-fixture-a'
      repository: 'vigneshrajsb/lifecycle-examples'
      branch: 'main'

services:
  - name: 'backend-svc-placeholder'
    docker:
      dockerImage: 'busybox'
      defaultTag: '1.36'
